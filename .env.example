# Local PostgreSQL. For Supabase copy the Session pooler URI from Connect,
# percent-encode the password, and use sslmode=require (or verify-full with CA).
DATABASE_URL=postgresql://transfercar:local_dev_only@localhost:5432/transfercar
# Optional: separate SELECT-only database role for the dashboard.
# DASHBOARD_DATABASE_URL=postgresql://...
